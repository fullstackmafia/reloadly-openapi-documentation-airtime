type: object
properties:
  content:
    type: array
    description: An array containing details of every discount accessible via Reloadly.
    items:
      type: object
      properties:
        operator:
          type: object
          properties:
            operatorId:
              description: Indicates the operator's identification number.
              type: string
              example: 
            name:
              description: Indicates the operator's name.
              type: string
              example: 
            countryCode:
              description: Indicates the ISO code of the country where the operator is registered.
              type: string
              example:
            status:
              description: Indicates if the operator has any existing discounts.
              type: boolean
              example: 
            data:
              description: Indicates if the operator has any existing data discounts.
              type: boolean
              example: 
            bundle:
              description: Indicates if the operator has any existing bundle discounts.
              type: boolean
              example: 
        percentage:
          description: Indicates the percentage discount for every top-up.
          type: integer
          example:
        internationalPercentage:
          description: Indicates the percentage discount for international top-ups.
          type: integer
          example:
        localPercentage:
          description: Indicates the percentage discount for local top-ups.
          type: integer
          example: 
        updatedAt:
          description: Indicates the time the discount was first created by the operator.
          type: integer
          example: 
          